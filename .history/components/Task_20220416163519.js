import React from "react";
import { View, Text, StyleSheet } from "react-native";

const Task = (this.props.first) => {
  return (
    <View>
      <Text>This is a task</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Task;
